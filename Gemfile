source "https://rubygems.org"

# GitHub Pages builds with its own pinned Jekyll; this gem is that exact set,
# so what builds here is what Pages publishes.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"  # Ruby 3+ needs it for `jekyll serve`
